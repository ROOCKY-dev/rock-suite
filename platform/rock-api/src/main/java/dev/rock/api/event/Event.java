package dev.rock.api.event;

/**
 * Base contract for everything published on the ROCK {@link EventBus}.
 * Events are the language of the platform (DMS "Domain Events").
 */
public interface Event {
}
